spring.application.name=billpayment

# Database connection properties
spring.datasource.url=jdbc:postgresql://localhost:5432/billpayment_db
spring.datasource.username=postgres
spring.datasource.password=power
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate properties
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

#
# default { username = user __ password = "you will find it in output log" }
# since i add users in SecurityConfig class, 
#   spring will not take the data from application.properties
#
#spring.security.user.name=root
#spring.security.user.password=root

#
# enable spring security logging for more info
#
#logging.level.org.springframework.security=DEBUG


spring.main.allow-bean-definition-overriding=true
logging.level.org.springframework.ws.server.MessageTracing=DEBUG
logging.level.org.springframework.ws.soap.saaj.SaajSoapMessage=DEBUG
